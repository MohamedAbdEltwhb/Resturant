<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mm.resturant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/NAppTheme">
        <activity android:name=".view.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activities.LoginActivity"
            android:theme="@style/NAppTheme" />
        <activity
            android:name=".view.activities.SignUpActivity"
            android:theme="@style/NAppTheme" />
        <activity
            android:name=".view.activities.MainActivity"
            android:theme="@style/NAppTheme" />
        <activity android:name=".view.activities.FoodActivity" />
        <activity android:name=".view.activities.WaitlistesTableActivity" />
        <activity android:name=".view.activities.DrinksActivity" />
        <activity android:name=".view.activities.DetailsFoodActivity"></activity>
    </application>

</manifest>